{
  "name": "dokku-snapshot-tests",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:integration": "vitest run tests/integration"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  }
}
